server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user-server?useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: info
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
